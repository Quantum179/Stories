import express from 'express'
let router = express.Router()
/* import {postMiddleware, getPosts, getPost, postPost} from './handlers'

router.get('/', getPosts)
router.get('/:idPost', getPost)
router.post('/', postPost)
 */
export default router
